#include <stdlib.h>

#include "controlcenter.h"


curve_t * curve_create(const char * path, gavl_time_t length, bg_msg_sink_t * sink)
  {
  curve_t * c = calloc(1, sizeof(*c));

  
  
  return c;
  }

void curve_append(curve_t * h, gavl_array_t * arr, const gavl_value_t * val)
  {
  
  }
